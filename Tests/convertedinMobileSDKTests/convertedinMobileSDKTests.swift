import XCTest
@testable import convertedinMobileSDK

final class convertedinMobileSDKTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(convertedinMobileSDK().text, "Hello, World!")
    }
}
